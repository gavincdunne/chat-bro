
package com.weekendware.chatbro.data.repository

import com.weekendware.chatbro.domain.model.CheckIn

class CheckInRepository {
    suspend fun save(checkIn: CheckIn) {
        // TODO: Implement save logic (e.g., DB or network)
    }
}
